% This Latex header sets bullets for enumerate lists
% Need to clear out Pandoc's default with reset-ordered-list-style.lua

\setlist[enumerate,1]{label=\arabic*.}
\setlist[enumerate,2]{label=(\alph*)}
\setlist[enumerate,3]{label=\roman*.}
